import { RelationDirective } from './relation.directive';

describe('RelationDirective', () => {
  it('should create an instance', () => {
    const directive = new RelationDirective();
    expect(directive).toBeTruthy();
  });
});
